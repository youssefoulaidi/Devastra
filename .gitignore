# Gradle / Android Studio
*.iml
.gradle/
.kotlin/
local.properties
.idea/
.DS_Store
build/
captures/
.externalNativeBuild/
.cxx/

# Build outputs (APKs/AABs are published to GitHub Releases, not committed)
*.apk
*.aab

# Signing material — never commit
*.keystore
*.jks
keystore.properties

# CI scratch files
gradle-debug.log
gradle-tail.txt
release-notes.md

# Bundled adhan audio: a curated set is committed for guaranteed offline playback;
# the build fetches the remaining voices into the same folder (best effort).
app/src/main/assets/adhan/*.part
